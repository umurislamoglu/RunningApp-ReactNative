import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  itemStyle: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: 5,
    color: '#000',
  },
  labelStyles: {
    fontWeight: 'bold',
    color: '#000',
  },
  valueStyles: {
    color: '#000',
  }
});
